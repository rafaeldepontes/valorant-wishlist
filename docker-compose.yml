services:
  postgres:
    image: postgres:latest
    container_name: "postgres"
    ports:
      - "5432:5432"
    env_file:
      - ".env"
    deploy:
      mode: replicated
      replicas: 1
    volumes:
      - postgres:/var/lib/postgresql

volumes:
  postgres:
